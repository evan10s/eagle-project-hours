import { TimeBetweenPipe } from './time-between.pipe';

describe('TimeBetweenPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeBetweenPipe();
    expect(pipe).toBeTruthy();
  });
});
